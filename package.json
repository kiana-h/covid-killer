{
  "homepage": "https://kiana-h.github.io/covid-killer/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webpack --mode development --watch",
    "build": "webpack --mode production",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^3.1.0",
    "webpack": "^5.11.0",
    "webpack-cli": "^4.2.0"
  }
}
